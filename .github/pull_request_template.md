## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `bun test` passes
- [ ] `bun run typecheck` passes
- [ ] If the build changed: `bun run build && node dist/cli.js --version` works
- [ ] If this touches `adopt`/`restore`: the write is backed up and restores byte for byte
- [ ] If this adds a config field or flag: the README is updated
